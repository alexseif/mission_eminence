<form method="post" action="{{ path('admin_mmm_delete', {'id': mmm.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mmm.id) }}">
	<button class="btn">Delete</button>
</form>
