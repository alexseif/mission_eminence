{% use 'bootstrap_5_layout.html.twig' %}

{% block form_label %}
	{% if required %}
		{% set label = label|default(name|humanize)|trans({}, translation_domain) ~ ' *' %}
	{% endif %}
	{{ parent() }}
{% endblock form_label %}
