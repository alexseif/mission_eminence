<article>

	<p class="card-text">{{ event.description }}</p>
	<div class="event-details">
		<p>
			<strong>Start:</strong>
			{{ event.start|date('Y-m-d H:i') }}</p>
		<p>
			<strong>End:</strong>
			{{ event.end|date('Y-m-d H:i') }}</p>
	</div>
</article>
